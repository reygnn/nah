<?xml version="1.0" encoding="utf-8"?>
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:settingsActivity="com.github.reygnn.nah.settings.SettingsActivity">
    <!-- Bewusst KEIN supportsSwitchingToNextInputMethod: nah hat keine Umschalt-Taste
         und ruft nie switchToNextInputMethod() — das Attribut wäre ein totes Versprechen.
         IME-Wechsel bleibt über System-Geste/IME-Picker möglich. -->
    <subtype
        android:imeSubtypeLocale="de_CH"
        android:imeSubtypeMode="keyboard"
        android:label="Deutsch (Schweiz) — Nah" />
</input-method>
