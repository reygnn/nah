<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".NahApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nah">

        <!-- Launcher-Icon → Einstellungen -->
        <activity
            android:name=".settings.SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Verwaltung der eigenen Wörter (aus den Einstellungen geöffnet) -->
        <activity
            android:name=".settings.UserWordsActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- Tipp-Training / Dojo (aus den Einstellungen geöffnet) -->
        <activity
            android:name=".settings.DojoActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- Die eigentliche IME -->
        <service
            android:name=".ime.NahIme"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>
</manifest>
